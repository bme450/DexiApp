<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.userauthentication" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UserAuthentication" >
        <activity android:name=".Instructions3" >
        </activity>
        <activity android:name=".Instructions2" />
        <activity android:name=".Instructions1" />
        <activity android:name=".Instructions" />
        <activity android:name=".Activity3b" />
        <activity android:name=".Activity3a"
            
            tools:ignore="Instantiatable" />
        <activity
            android:screenOrientation="landscape"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:name=".Activity2" />
        <activity android:name=".Activity1" />

        <activity android:name=".Activities" />
        <activity android:name=".A_Mainpage" />
        <activity android:name=".A_MainActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".MainActivityLogin" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>